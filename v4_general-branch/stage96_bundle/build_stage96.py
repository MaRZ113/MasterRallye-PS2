from pathlib import Path
import shutil, pandas as pd
# placeholder build script for Stage 96 bundle regeneration
print("Stage 96 bundle is data-first; regenerate from stage95 extracts and current manifests.")